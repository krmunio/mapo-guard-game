# 파인콘 GitHub Copilot 부스 대학생 운영 가이드

이 문서는 대학생 운영자가 초등학생 참가자와 **마포 수호대 미니게임** 체험을 8~10분 동안 안전하고 일관되게 진행하기 위한 현장 가이드입니다.

> [!NOTE]
> 이 문서는 Linux 노트북 기준의 공식 현장 운영 문서입니다. 체험의 교육적 흐름, 실제 파일 경로, 프롬프트를 모두 이 가이드에서 확인할 수 있습니다.

## 운영 목표

이 부스는 코딩 문법을 가르치는 시간이 아닙니다. 참가자가 다음 세 가지를 경험하면 성공입니다.

1. 만들고 싶은 것을 자연어로 구체적으로 설명합니다.
2. GitHub Copilot이 제안한 변경 사항을 사람과 함께 확인합니다.
3. 실행 결과를 검토하고 더 정확하게 다시 요청합니다.

> [!IMPORTANT]
> 핵심 메시지는 **"AI가 만든 결과도 사람이 확인해야 한다"**입니다. Copilot을 정답을 주는 자동 조종사가 아니라, 사람의 설명을 듣고 함께 만드는 조종 보조원으로 소개합니다.

## 운영자가 알아둘 파일

| 용도 | 경로 |
| --- | --- |
| 참가자가 수정할 게임 | [`apps/`](apps/) |
| 주로 수정할 설정 | [`apps/script.js`](apps/script.js)의 `gameConfig` |
| 정상 동작 완성본 | [`apps/examples/completed/`](apps/examples/completed/) |
| 점수 오류 탐정 미션 | [`apps/examples/debug-mission/`](apps/examples/debug-mission/) |

참가자 체험에서는 가능한 한 `gameConfig`만 수정합니다. 다른 게임 로직을 바꾸면 다음 참가자를 위한 초기화와 오류 대응이 어려워질 수 있습니다.

## 행사 전 준비

### 기기와 계정

- [ ] Linux 노트북에 Git, Visual Studio Code, Chrome 또는 Edge가 설치되어 있습니다.
- [ ] VS Code에 GitHub Copilot 확장이 설치되어 있습니다.
- [ ] 주최 측 체험용 GitHub 계정으로 로그인되어 있습니다.
- [ ] Copilot Chat에 `안녕하세요`를 입력했을 때 정상적으로 응답합니다.
- [ ] 참가자가 개인 GitHub 계정을 만들거나 로그인하지 않도록 설정했습니다.

### 프로젝트 준비

터미널에서 저장소를 열고 최신 상태로 준비합니다.

```bash
git clone https://github.com/krmunio/mapo-guard-game.git
cd mapo-guard-game
code .
```

이미 저장소가 있다면 다음 명령을 사용합니다.

```bash
cd <mapo-guard-game 저장소 경로>
git pull
code .
```

> [!CAUTION]
> `git pull`이 로컬 변경 때문에 실패하면 파일을 임의로 삭제하거나 강제로 되돌리지 말고 행사 책임자에게 확인합니다.

### 게임 사전 점검

1. 저장소 루트의 터미널에서 체험 작업본을 정상 완성본으로 초기화합니다.

   ```bash
   bash apps/reset.sh
   ```

2. 체험 게임을 엽니다.

   ```bash
   xdg-open apps/index.html
   ```

   `xdg-open`을 사용할 수 없다면 VS Code 탐색기에서 [`apps/index.html`](apps/index.html)을 찾아 파일 관리자에 표시한 뒤 설치된 브라우저로 엽니다.

3. 한강공원의 초록 로봇이 표시되는지 확인한 후 캐릭터를 다섯 번 선택해 다음 동작을 확인합니다.
   - 목표물 하나당 10점 증가
   - 목표물 하나씩 감소
   - 50점에서 임무 성공 메시지 표시
   - **다시 시작** 버튼으로 0점 상태 복원
4. [`apps/examples/debug-mission/index.html`](apps/examples/debug-mission/index.html)을 열어 목표물 하나당 1점이 증가하는지 확인합니다.
5. 한글 입력, 화면 확대 비율, 마우스와 키보드 조작을 확인합니다.
6. 캐릭터·색깔·능력·장소 카드와 개선 미션 카드를 준비합니다.

## 교대 시작 체크리스트

- [ ] 브라우저에는 [`apps/index.html`](apps/index.html)이 열려 있습니다.
- [ ] VS Code에는 [`apps/script.js`](apps/script.js)가 열려 있습니다.
- [ ] Copilot Chat이 열려 있고 체험용 계정으로 로그인되어 있습니다.
- [ ] 게임은 한강공원의 초록 로봇이며, 점수는 0점이고 목표물은 5개입니다.
- [ ] 이전 참가자의 프롬프트와 개인정보가 화면에 남아 있지 않습니다.
- [ ] 정상 완성본과 탐정 미션의 위치를 알고 있습니다.

## 8~10분 진행 순서

### 1. 아이디어 선택 - 1분

참가자가 카드에서 하나씩 선택하게 합니다.

| 항목 | 선택할 수 있는 값 |
| --- | --- |
| 캐릭터 | 고양이, 공룡, 토끼, 로봇 |
| 색깔 | 파랑, 분홍, 초록, 무지개 |
| 능력 | 별 모으기, 쓰레기 치우기, 나무 심기 |
| 장소 | 한강공원, 도서관, 우주, 마법의 숲 |

시작 멘트:

> GitHub Copilot은 코딩할 때 옆에서 도와주는 AI 친구예요. 하지만 마음을 읽을 수는 없어서 무엇을 만들고 싶은지 자세히 알려줘야 해요.

참가자가 아이디어를 고르기 어려워하면 운영자가 대신 정하지 말고 카드 한 장씩을 고르게 합니다.

### 2. Copilot에게 요청 - 2분

저학년은 운영자가 입력하고, 고학년은 참가자가 직접 한두 문장을 입력하도록 돕습니다.

아래 형식을 공식 운영 프롬프트로 사용합니다.

```text
script.js의 gameConfig에서 주인공을 초록색 로봇으로 바꿔줘.
장소는 한강공원, 능력은 쓰레기 치우기로 바꾸고 어울리는 이모지도 설정해줘.
다른 게임 로직은 바꾸지 마.
```

Copilot이 변경을 제안하면 바로 적용하지 말고 참가자와 함께 다음을 확인합니다.

- 선택한 캐릭터, 색깔, 능력, 장소가 모두 포함되었나요?
- 수정 대상이 `apps/script.js`인가요?
- `gameConfig` 이외의 게임 로직을 불필요하게 바꾸지 않았나요?

확인 멘트:

> Copilot이 코드를 제안했어요. 바로 믿지 않고 우리가 부탁한 내용과 같은지 먼저 살펴볼게요.

### 3. 실행하고 검토 - 2분

브라우저를 새로 고치고 참가자가 캐릭터를 직접 선택하게 합니다.

다음 질문을 순서대로 합니다.

1. 우리가 부탁한 캐릭터와 장소가 맞나요?
2. 캐릭터를 누르면 원하는 행동이 보이나요?
3. 점수가 한 번에 10점씩 올라가나요?
4. 이상하거나 한 번 더 바꾸고 싶은 부분이 있나요?

결과가 예상과 다르면 실패로 표현하지 않고 관찰한 차이를 구체적인 문장으로 바꿉니다.

### 4. 한 번 더 개선 - 1~2분

시간 안에 확인할 수 있는 개선 사항 하나만 선택합니다.

- 캐릭터가 더 높이 점프하게 하기
- 점수가 오를 때 별 효과 추가하기
- 배경색 바꾸기
- 성공 문구 바꾸기

예시:

```text
점수가 50점이 되면 별가루 효과와 함께
"마포 수호대 임무 성공!"이라고 크게 보여줘.
기존 점수 계산 방식은 바꾸지 마.
```

> [!TIP]
> 제한 시간, 새로운 화면, 외부 라이브러리처럼 변경 범위가 큰 요청은 체험 시간이 부족해질 수 있습니다. 한 번에 눈으로 확인할 수 있는 작은 변화로 유도합니다.

### 5. AI 탐정 미션 - 2분

[`apps/examples/debug-mission/index.html`](apps/examples/debug-mission/index.html)을 열고 캐릭터를 눌러 점수가 1점만 증가하는 모습을 보여줍니다.

```text
점수가 10점씩 올라가야 하는데 1점씩 올라가고 있어.
원인을 초등학생도 이해할 수 있게 설명하고 고쳐줘.
```

강조할 멘트:

> AI에게 그냥 "고쳐줘"라고 하기보다 무엇이 이상한지 정확히 알려주면 더 잘 도와줘요.

대기 줄이 길거나 앞 단계가 오래 걸렸다면 개선 단계와 탐정 미션 중 하나만 진행합니다.

### 6. 마무리 - 1분

참가자에게 다음 세 가지를 질문합니다.

1. 무엇을 만들고 싶다고 설명했나요?
2. Copilot이 무엇을 도와줬나요?
3. AI가 만든 결과는 누가 확인해야 할까요?

마지막 답은 **사람**이 되도록 유도합니다.

마무리 멘트:

> 오늘 코드를 모두 외운 것이 중요한 게 아니라, 원하는 것을 설명하고 결과를 확인하는 방법을 체험한 거예요.

## 프롬프트 작성 도와주기

참가자가 막히면 다음 문장을 채우게 합니다.

```text
[무엇]을 [어떻게] 바꿔줘.
사용자가 [행동]하면 [결과]가 나타나게 해줘.
화면에는 "[문구]"를 보여줘.
다른 게임 로직은 바꾸지 마.
```

| 피할 요청 | 구체적인 요청 |
| --- | --- |
| `재미있게 만들어줘.` | `로봇을 누르면 위로 점프하게 해줘.` |
| `예쁘게 바꿔줘.` | `배경을 짙은 파란색 밤하늘로 바꿔줘.` |
| `오류를 고쳐줘.` | `10점씩 올라가야 하는데 1점씩 올라가. 원인을 설명하고 고쳐줘.` |

Copilot이 복잡한 변경을 제안하면 다음처럼 범위를 줄입니다.

```text
외부 라이브러리를 추가하지 말고 현재 HTML, CSS, JavaScript 안에서 간단하게 수정해줘.
```

## 연령별 진행 방법

| 대상 | 권장 방식 |
| --- | --- |
| 초등 1~3학년 | 참가자는 카드를 고르고 실행 결과를 확인합니다. 운영자가 프롬프트를 입력합니다. |
| 초등 4~6학년 | 참가자가 직접 한두 문장을 입력하도록 돕습니다. |
| 학부모 동반 | 보호자가 조건 하나를 추가하고 아이가 결과를 검토하게 합니다. |
| 코딩 경험자 | 직접 프롬프트를 작성하고 탐정 미션의 원인까지 설명하게 합니다. |

## 문제 발생 시 대응

| 상황 | 대응 |
| --- | --- |
| Copilot 응답이 늦음 | 30초 이상 지연되면 정상 완성본을 열고 변경 전후를 비교하는 설명형 체험으로 전환합니다. |
| 인터넷 연결이 끊김 | 게임은 계속 실행할 수 있습니다. 완성본과 탐정 미션을 이용해 AI의 제안과 사람의 검토 과정을 설명합니다. |
| 코드 실행 오류 | 브라우저와 VS Code의 오류 메시지를 확인하고, 메시지와 기대 동작을 함께 Copilot에 전달합니다. |
| 결과가 요청과 다름 | 실제 결과와 기대 결과의 차이를 한 문장으로 적어 다시 요청합니다. |
| 변경 범위가 너무 큼 | `gameConfig만 수정해줘` 또는 `외부 라이브러리 없이 간단하게 수정해줘`라고 범위를 줄입니다. |
| 참가자가 아이디어를 못 정함 | 캐릭터, 색깔, 능력, 장소 카드를 한 장씩 고르게 합니다. |
| 대기 줄이 길어짐 | 개선 단계와 탐정 미션 중 하나만 진행하고 8분 안에 마무리합니다. |
| 부적절한 문구를 입력함 | 다른 참가자가 함께 보는 공간임을 알리고 안전한 행사 주제로 전환합니다. |
| 이전 참가자의 변경이 남음 | 아래 초기화 명령을 실행한 뒤 브라우저를 새로 고칩니다. |

오류가 발생했을 때 사용할 멘트:

> 실패한 것이 아니라 AI에게 더 정확한 단서를 줄 기회가 생긴 거예요.

## 다음 참가자를 위한 초기화

현재 참가자의 체험이 끝나면 저장소 루트의 터미널에서 다음 명령을 실행합니다.

```bash
bash apps/reset.sh
```

이 스크립트는 [`apps/examples/completed/`](apps/examples/completed/)의 `index.html`, `style.css`, `script.js`가 모두 있는지 확인한 뒤 체험 작업본을 한강공원의 초록 로봇 완성 버전으로 되돌립니다.

> [!WARNING]
> 초기화 명령은 참가자가 수정한 `index.html`, `style.css`, `script.js`를 덮어씁니다. 결과를 보관해야 한다면 먼저 행사 책임자에게 확인합니다.

초기화 후 다음 순서로 확인합니다.

1. 브라우저에서 [`apps/index.html`](apps/index.html)을 새로 고칩니다.
2. 한강공원의 초록 로봇, 점수 0점, 목표물 5개인지 확인합니다.
3. Copilot Chat 상단의 **새 채팅** 버튼을 눌러 빈 대화를 시작하고 이전 참가자의 내용이 보이지 않는지 확인합니다.
4. 참가자가 입력한 개인정보나 별도 생성 파일이 남아 있지 않은지 확인합니다.
5. VS Code에서 [`apps/script.js`](apps/script.js)를 다시 엽니다.

## 개인정보와 안전 원칙

- 초등학생이 개인 GitHub 계정을 만들거나 개인 계정으로 로그인하게 하지 않습니다.
- 주최 측에서 준비한 체험용 계정과 기기만 사용합니다.
- 실명, 학교명, 연락처, 주소, 사진 등 개인정보를 프롬프트에 입력하지 않습니다.
- 이름이 필요하면 `별토끼`, `초록로봇` 같은 행사 전용 닉네임을 사용합니다.
- 참가자가 개인정보를 입력하려 하면 적용을 중단하고 해당 내용을 제거합니다.
- Copilot이 만든 내용은 틀리거나 부적절할 수 있으므로 운영자가 적용 전에 확인합니다.
- 참가자의 아이디어를 평가하거나 정답을 강요하지 않습니다.
- 운영자가 코드를 대신 완성하는 모습을 오래 보여주지 않습니다.

## 교대 종료 체크리스트

- [ ] 체험 작업본을 정상 완성본으로 초기화했습니다.
- [ ] 브라우저의 게임 점수와 목표물이 초기 상태입니다.
- [ ] Copilot Chat에 참가자의 대화나 개인정보가 남아 있지 않습니다.
- [ ] 참가자가 만든 별도 파일이나 다운로드가 남아 있지 않습니다.
- [ ] 다음 운영자에게 계정, 네트워크, 기기 이상을 전달했습니다.

## 관련 문서

- [프로젝트 실행 및 설정 안내](README.md)
- [GitHub 이용약관](https://docs.github.com/en/site-policy/github-terms/github-terms-of-service)
- [GitHub Copilot 기능 안내](https://docs.github.com/en/copilot/get-started/features)
